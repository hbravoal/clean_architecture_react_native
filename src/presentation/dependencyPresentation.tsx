import { container } from 'tsyringe';

export const DependencyInjectionPresentation = (): void => { 
    /*
    container.register('IHomeApplication', {
        useClass: HomeApplication
      })
      */
}
export default DependencyInjectionPresentation;